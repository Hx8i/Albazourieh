# NestJS API base URL
NEXT_PUBLIC_API_URL=http://localhost:4000/api/v1

# That's all. Staff authentication is JWT-based (email + password against
# the backend's MunicipalityUser table), and file uploads go through the
# backend's validated /uploads pipeline — no client-side secrets needed.
